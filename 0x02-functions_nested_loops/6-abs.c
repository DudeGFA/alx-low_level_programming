#include "main.h"

/**
 * int _abs - prints the abs of a number
 * @n : character to be checked
 * Return: 0 or 1
 */
int _abs(int n)
{
	if (n > 0)
	{
		n = n
	}
	else if (n < 0)
	{
		n = n*(-1);
	}
	else
	{
		n = 0
	}
	return (n);
}

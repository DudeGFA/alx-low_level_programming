/**
 * add - adds two ints
 * @a: first int
 * @b: second int
 * Return: a + b
 */
int add(a, b)
{
	return (a + b);
}
/**
 * sub - finds diff of two ints
 * @a: first int
 * @b: second int
 * Return: a - b
 */
int sub(a, b)
{
	return (a - b);
}
/**
 * mul - multiplies two ints
 * @a: first int
 * @b: second int
 * Return: a * b
 */
int mul(a, b)
{
	return (a * b);
}
/**
 * div - divides two ints
 * @a: first int
 * @b: second int
 * Return: a / b
 */
int div(a, b);
{
	return (a / b);
}
/**
 * mod - return remainder of a / b
 * @a: first int
 * @b: second int
 * Return: a // b
 */
int mod(a, b)
{
	return (a // b);
}
